// import CONSTANTS from './../constants';

// const initialState = []

// const sliderReducer = (state = initialState, action) => {
//     switch(action.data) {
//         case CONSTANTS.ACTION_TYPES.SLIDER.FORWARD_IMAGE: {
//             console.log("state of index", this.state.index)
//             return {...state,index: (this.state.index + 1) % state.length }
//         }
//         case CONSTANTS.ACTION_TYPES.SLIDER.BACKWARD_IMAGE: {
//             return {...state, sliderItem: action.item}
//         }
//         default: {
//             return state;
//         }
//     }
// }
// export default sliderReducer;
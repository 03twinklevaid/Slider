
const CONSTANTS = {
    ACTION_TYPES: {
        POP_UP: {
            SHOW_POP_UP: 'SHOW_POPUP',
            HIDE_POP_UP: 'HIDE_POPUP'
        },
        HOME: {
            ON_HOME_SUCCESS: 'HOME_SUCCESS'
        },
        SLIDER: {
            FORWARD_IMAGE: 'MOVE_FORWARD',
            BACKWARD_IMAGE: 'MOVE_BACKWARD'
        },
        FILTER: {
            SELECT_CATEGORY: 'SELECT_CATEGORY'
        },
        PRICE_FILTER: {
            PRICE_FILTER: 'PRICE_FILTER'
        }
    }
}

export default CONSTANTS;